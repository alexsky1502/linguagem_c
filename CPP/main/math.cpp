#include <iostream>
#include <cmath>
using namespace std;

int main(void) {
    //MAX
    cout << max(10,5);
    //MIN
    cout << min(10,5);
    //SQRT ROUND AND LOG
    cout << sqrt(64);
    cout << round(2.6);
    cout << log(2);
}